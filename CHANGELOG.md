# 変更履歴（CHANGELOG）

Stock Auto Tagger の主な変更点をまとめています。**新しい変更ほど上**に記載します。

各サイト（Adobe Stock / Shutterstock / Pixta）はページの作りが予告なく変わることがあり、その都度アップロード・提出処理を修正しています。アップデートがうまく動かないときは、まずこのページで最近の修正内容をご確認ください。

> 記号の意味: 🆕 追加機能 / 🛠 修正 / 🔧 変更・改善 / 🔒 セキュリティ / 📖 ドキュメント

---

## 2026-09-10

### 🔧 ベクター: Pixta 用 ZIP は利用者が用意する方式に変更（**入れ方が変わります**）

Pixta のベクター入稿は「対応する JPEG と EPS をまとめた ZIP。PNG は任意の追加」で、
**背景が透過かどうかで中身が変わります。** これまでツールが EPS と PNG を機械的に ZIP 化していましたが、
不透過の素材では PNG が JPEG と同じ絵になり、Pixta の要件から外れていました。

**これからは、素材ごとのフォルダに ZIP をご自身で置いてください。** ツールはそれをそのまま送ります。

```
input/Vector/260402_icon/
    ├── 260402_icon.eps   ← Adobe / Shutterstock はこれをそのまま送ります
    ├── 260402_icon.jpg   ← Pixta の ZIP に入れる見本画像
    ├── 260402_icon.png   ← 背景が透過の素材のときだけ
    └── 260402_icon.zip   ← ★Pixta へ送る ZIP。あなたが作ります★
```

| 素材 | ZIP に入れるもの |
|---|---|
| 背景が透過 | EPS + PNG + JPEG |
| 背景に色や白を敷いている | EPS + JPEG |

- JPEG・PNG は**その EPS を開いて実寸で書き出したもの**にしてください。Pixta は ZIP の中の EPS と見本画像の見た目が違うとリジェクトします
- ZIP が無い素材は Adobe と Shutterstock にだけ上がり、Pixta はスキップされます（ログに警告が出ます）
- Adobe は ZIP を受け付けません。ZIP は Pixta 専用です
- EPS は Illustrator 10 形式で保存してください（Shutterstock は EPS 8 か 10 のみ、Pixta は Illustrator 8.0〜CS2 互換のみ。EPS 8 はメタデータを保持しません）

### 📖 ドキュメント

- README・マニュアルの「Vector フォルダの入れ方」に、ZIP の中身と作り方を追記しました

### 🛠 表示の修正

- ベクターだけをアップロードする日に、Pixta の確認ダイアログが「以下の0件をPixtaにアップロードします」と表示されていた問題を修正しました（ZIP を件数とファイル一覧に含めます）

---

## 2026-08-08

- 🛠 **写真は全件提出できているのに「未提出が残っています」と警告される問題を修正（Shutterstock）**
  「未送信」の件数表示は**写真と動画を合わせた全体の残り件数**で、開いているタブだけの件数ではありませんでした。そのため写真を提出した直後にこの数字を見ると、**これから提出する動画の分が必ず残って見え**、写真の提出に失敗したと誤って判定していました（例: 写真6件・動画1件の日に「写真 未送信 (1)」と警告。実際はその直後に動画も提出できて残り0件）。提出の成否は**写真と動画の提出が両方終わってから1回だけ**判定するようにし、残っていた場合も種別を決めつけず「未送信」として報告します。2026-08-07 の修正（件数が0になるまで待つ）では防げていなかった原因です。

## 2026-08-07

- 🛠 **提出できているのに「未提出が残っています」と誤って警告される問題を修正（Shutterstock）**
  提出ボタンを押した直後は「未送信」タブの件数表示がまだ更新されておらず、提出前の数字が残っていることがあります。これまでは4秒待って1回読むだけだったため、**実際は全件提出できているのに未提出が残っていると警告**していました。件数が0になるまで画面を再読み込みしながら最大90秒待つようにしました。件数そのものを読み取れなかった場合も、これまでのように「未提出あり」とはせず「確認できなかった」として区別します。

- 🆕 **提出できずに「未送信」に残った素材を検知するようにしました（Shutterstock）**
  カテゴリー未設定などで提出条件を満たしていないと、提出ボタンを押しても素材は未送信のまま残ります。これまでは残っていても完了扱いのログが出るだけで、**気づかないまま何日も溜まり続ける**ことがありました。残った場合は最後のまとめに「要手動対応」として表示します（ファイルはアップロード済みのため、再アップロードは不要です）。

- 🛠 **メタデータCSVが効いていないのに「反映OK」と表示される問題を修正（Shutterstock）**
  反映確認をキーワードの「個数」だけで行っていました。Pixta 向けに画像へ埋め込まれた日本語キーワードを Shutterstock が読み込むため、**CSVが一度も適用されていなくてもキーワードは表示され、確認が通ってしまう**状態でした。表示中のキーワードがCSVの英語キーワードと一致するかで判定するようにし、一致が0件のときは警告するようにしました。

- 🛠 **ヘッダーに引用符が付いたCSVが丸ごと無視される問題に対応**
  Adobe Stock / Shutterstock は列名の完全一致で判定するため、`"Filename","Description",...` のように引用符付きのヘッダーになっていると**CSV全体が無視され、説明・キーワード・カテゴリーが未適用のまま提出できなくなります**（Excel で開いて保存し直すと起こりえます）。アップロード前にヘッダー書式を自動で修復するようにしました。列名そのものが違う場合は書き換えず警告のみ出します。

- 🔧 **テストモードで素材を選択した状態で停止するようにしました（Shutterstock）**
  審査提出ボタンは素材が選択されているときだけ表示されるため、これまでは停止後に手動で提出しようとしてもボタンが出ませんでした。

## 2026-08-01

- 🛠 **動画のタイトル・キーワードが入らないまま「未送信」に残り続ける問題を修正（Shutterstock）**
  メタデータCSVの適用を画像側の画面でしか行っていませんでした。「CSVをアップロード」ボタンはその画面に素材が無いと表示されないため、**その日に画像が1枚も無く動画だけをアップロードすると、動画にCSVが適用されないまま**になり、説明・キーワードが空で提出できず未送信に溜まり続けていました。動画側の画面でもCSVを適用するようにしました。

- 🛠 **Pixta が混雑しているときにアップロードが原因不明のエラーで止まる問題を修正**
  Pixta はアクセスが集中すると、アップロード画面の代わりに「ただいま、アクセスが集中しております」という案内ページを返すことがあります。これまではそれを検知できず「アップロードボタンが見つからない」というエラーで即座に失敗していました（同じ実行の中でイラストだけ失敗し動画は成功する、といった分かりにくい形で出ます）。混雑ページを検出したら間を置いて自動で開き直すようにしました（最大4回・45秒間隔）。イラスト・写真・動画のすべてに適用されます。

## 2026-07-24

- 🛠 **メタデータCSVが複数あると、最新の1枚以外が無視されてタイトル・タグが空のまま提出される問題を修正**
  csv_output フォルダに同じサイト向けのCSVが複数たまっていると、これまでは一番新しい1枚だけが使われ、他のCSVに載っている素材はメタデータ未適用のままアップロードされていました。アップロード開始時に全CSVを自動で1枚に統合（同じファイル名の行は新しい方を優先、統合後の古いCSVはゴミ箱へ）するようにしました。Adobe Stock / Shutterstock の一括・単体アップロードすべてに適用されます。

- 🛠 **Adobe Stock でアップロードに失敗したファイルがあると、反映待ちで最大35分待たされる問題を修正**
  Adobe 側で受け付けられなかったファイル（動画が短すぎる等）は一覧に永遠に現れないため、これまでは反映待ちがタイムアウト（最大30分＋再待機5分）まで待ち切っていました。ページを更新しても反映数が増えない状態が続いたら「残りはアップロード失敗」と判断して数分で切り上げ、反映済みのファイルだけで処理を続行するようにしました。正常時の動作は変わりません。

## 2026-07-20

- 🛠 **Adobe Stock で提出に成功したのに「提出できていない」と誤って表示される問題を修正**
  提出の成否を「アップロード一覧からファイルが消えたか」で判定していましたが、提出済みでも一覧にしばらく残り続けることがあり、成功を「要手動対応」と誤判定していました。ポータルの「審査中」一覧に該当ファイルが現れたことを直接確認する方式に変更しました。
- 🛠 **Adobe で人間確認画面が出た後、Shutterstock / Pixta が必ずエラーになる問題を修正**
  Adobe のブラウザを開いたままにすると、内部の実行環境が残ったままになり、続く Shutterstock / Pixta の起動が「Sync API inside the asyncio loop」エラーで全て失敗していました。サイトごとに実行環境を分離して、Adobe の手動対応待ち中でも後続サイトのアップロードが正常に動くようにしました。
- 🛠 **Adobe Stock で提出に失敗しても通知されずに処理が進んでしまう残りのケースを修正（レビュー指摘の反映）**
  審査登録ボタンが見つからない等の理由で提出できなかった場合に「提出完了 0件」とだけ表示されて気づけないケースが残っていたため、人間確認画面と同じ「要手動対応」ポップアップでお知らせするようにしました。メタデータCSVの適用が途中で止まった場合もエラーとして通知します。
- 🔧 **人間確認画面の誤検出を防止**
  ページに常駐する見えない確認部品まで検出して、正常に提出できているのに「要手動対応」と表示される可能性があったため、実際に画面に表示されている確認画面だけを検出するようにしました。
- 🔧 **提出完了の確認待ちを最大約2分→約6分に延長**
  ファイル数が多いときに、Adobe 側の反映が遅いだけで「未提出」と判定されるのを防ぎます。時間切れの場合の案内文にも「反映が遅れているだけの可能性がある」ことを明記しました。
- 🔧 テストモードでは完了表示を「メタデータ適用 N件」に変更（実際には提出していないのに「提出完了 N件」と表示されていました）。
- 🛠 **Adobe Stock が実際には審査提出できていないのに「提出完了」と表示されていた問題を修正**
  Adobe は人間かどうかを確認するため、提出の直前に画像選択（猫の画像を選ぶなど）や単語入力の確認画面をランダムに表示することがあります。これが出るとファイルはアップロードされても審査提出は完了しませんが、これまでは完了として報告し、そのままファイル移動まで進んでしまっていました。提出後に実際に受け付けられたかを確認し、できていない場合は件数を「0件」として正しく報告します。
- 🆕 **人間確認画面が出たらポップアップでお知らせするようにしました**
  自動実行中は別の作業をしていて気づけないため、最前面のポップアップで通知します。ブラウザは開いたままにするので、そのまま画像選択などに対応して手動で審査提出を完了してください。
  - **Adobe 単体で実行した場合**: その場でポップアップを表示し、手動対応待ちの状態を保ちます
  - **全工程を実行した場合**: いったんログに記録して Shutterstock・Pixta の処理を続行し、最後にまとめてポップアップでお知らせします（ファイルは Adobe にアップロード済みのため、ファイル移動は通常どおり行われます）
- 🔧 **単語入力の確認画面へ自動で回答するのをやめました**
  これまでは画像の内容と関係のない固定の単語を入力して送信していました。誤った回答を繰り返すとアカウントの評価に影響する可能性があるため、自動回答は廃止し、人間に引き継ぐ方式に統一しました。
- 📖 マニュアル（工程2・トラブルシューティング）と README に、Adobe の人間確認画面が出たときの動作と対処を追記しました。

## 2026-07-02

- 🔒 **セキュリティ強化（外部レビュー結果の反映）**
  - Pixta 動画のタグ入力（クリップボード経由）で、AI が生成したタグをコマンドに直接埋め込まない方式に変更（コマンドインジェクション対策・日本語タグの文字化け防止）
  - Gemini API キーを URL ではなくヘッダーで送信するように変更（通信ログ等へのキー露出を防止）
  - 工程0リネームで AI の応答をファイル名に使う前に英数字のみへ正規化（想定外の文字混入対策）
  - 画像メタデータ（XMP）埋め込み時にタイトル・キーワードの特殊文字を正しくエスケープ
  - 起動時ログの API キー表示を「読み込み済み」のみに変更（一部マスク表示を廃止）
- 🛠 **安定性の改善**
  - テストモードの全自動実行を繰り返すとブラウザ制御プロセスが溜まっていく問題を修正（再実行時・アプリ終了時に解放）
  - Gemini へのファイルアップロードが一時的な通信エラーで即失敗していたのを、自動リトライするように改善
- 🔧 **整理**
  - 使われていなかった FTPS アップロード処理を削除（実際のアップロードはブラウザ自動化のみ）
  - `requirements.txt` の各パッケージにバージョン範囲を指定（インストール時の予期しない非互換を防止）
- 📖 README のフォルダ構成説明を実態に合わせて修正（`variation/01〜10` は手動作成であることを明記、`config.json` サンプルに `test_mode` を追加）

## 2026-06-22

- 🛠 **Shutterstock にアップロードできない問題を修正**
  Shutterstock 出品ページの仕様変更に追従しました。次の不具合を解消しています。
  - 「アップロードボタンが見つかりません」で止まる → 新しいファイル選択方式（ドロップゾーン）に対応
  - 審査提出が「提出ボタンが見つかりません」で止まる → 提出ボタンが素材選択中のみ表示される新仕様に対応
  - メタデータ確認の途中でページが再読み込みされ、選択が外れて提出できなくなる問題を解消

## 2026-04-28

- 🛠 Gemini のバッチ解析で枚数が不足したとき、単発で取り直す（リトライ）ように改善
- 🛠 Adobe のタイトルが空のときにアプリが落ちる不具合を修正

## 2026-04-21 〜 04-22

- 🔧 ファイル移動（工程5）を、処理結果の記録に頼らず「残っているファイルを全部見て移動する」方式に変更（AI／Photo サブフォルダも移動対象に含めるよう改善）

## 2026-04-15

- 🔒 **機密ファイルの保存場所をプロジェクト外（ホームディレクトリ）に移動**し、フォルダ構成を OS 別に再編
  ※ これより前に入手した方は [MIGRATION.md](./MIGRATION.md) を必ずご確認ください
- 🔧 起動時のログインセッション確認を「1サイト以上あれば起動可」に緩和
- 🛠 一部処理が内部変数の不備で落ちる不具合を修正
- 📖 README／HTML マニュアルを実際の挙動に合わせて整理

## 2026-04-02 〜 04-14（初期リリース期）

- 🆕 **Stock Auto Tagger 公開**
- 🆕 Gemini AI による画像・動画のタイトル／キーワード／カテゴリ自動生成（最大10枚ずつのバッチ解析で API 消費を削減）
- 🆕 Adobe Stock / Shutterstock / Pixta への一括アップロード〜審査提出（Playwright によるブラウザ自動操作）
- 🆕 Adobe／Shutterstock 用 CSV 出力、Pixta 用 IPTC／XMP メタデータ埋め込み
- 🆕 色違いバリエーション素材の自動リネーム、ベクター（EPS）対応、全自動パイプライン
- 🆕 Mac／Windows 用アプリランチャー
- 🔧 Pixta の AI 素材受付停止に対応（AI フォルダを Pixta 除外に）、禁則文字の自動サニタイズ、テストモードの既定 ON 化 など
